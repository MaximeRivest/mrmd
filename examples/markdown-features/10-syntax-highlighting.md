# Syntax Highlighting

Add a **language** identifier after the opening *backticks* for syntax highlighting.

## JavaScript

```javascript
function hello(name) {
  console.log(`Hello, ${name}!`);
  return true;
}

const result = hello("World");
```

## Python

```python
def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n-1) + fibonacci(n-2)

for i in range(10):
    print(fibonacci(i))
```

## CSS

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
  background-color: #f0f0f0;
}
```

## HTML

```html
<!DOCTYPE html>
<html>
<body>
  <h1>Hello World</h1>
  <p>This is HTML fdsfd fdsfs fsdfsdfdsfsfds</p>
</body>
</html>
```
